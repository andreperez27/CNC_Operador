/**
 * Geometria de contato — re-export do módulo canônico (core).
 * Mantém a API legada: buildContactGeometry, validateContact.
 */

export {
  buildContactGeometry,
  validateContact,
} from '../../../core/machining/contactGeometry';