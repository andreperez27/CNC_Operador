/**
 * Catálogo de ferramentas — re-export do catálogo canônico (core).
 * Mantém a API legada: TOOL_TYPES, TOOL_TYPE_LIST, getToolType.
 */

export { TOOL_TYPES, TOOL_TYPE_LIST, getToolType, isKnownToolType } from '../../../core/tools/toolTypes';