# Diário de bordo — 2026-09-24

## Núcleo do cabeçote Huron validado + aba com preview + calibração do anel

Ponto de partida do diário: registra o trabalho das últimas sessões, ainda
não documentado em lugar nenhum.

- **Implementado:** núcleo `calculateHuronFlanges({A,B,C})` validado contra
  10 leituras reais da máquina (45 Huron, duas flanges de 45°); aba
  "Cabeçote Huron" com formulário A/B/C, `ResultBox`, `CopyButton` e preview
  SVG em estilo de desenho técnico (perfil + topo com cotas); compensação do
  anel de graduação (`applyRingCalibration`, normalização 0–360°); seletor de
  4 posições (0°/90°/180°/270°) com persistência em `localStorage`;
  calibração identificada por máquina (`huron-calibracao:<id>`) com migração
  das chaves antigas; identificador renomeado pra `feller-huron45` (modelo
  específico do Portal Feller, não genérico).
- **Corrigido:** preview inicial abstrato substituído pelo desenho técnico
  aprovado; campos numéricos trocados pra aceitar menos no mobile
  (`inputMode="decimal"` + botão +/− à esquerda + vírgula decimal);
  fallback de clipboard endurecido pro iOS; teste de paridade do chanfro
  deixou de passar vacuamente (fixture vendorizado no repo).
- **Decisões:** sem desvio de anel, o preview e o resultado usam os valores
  compensados (anel físico só lê 0–360°); aviso de `bFlange = 0` baseado no
  valor teórico; segunda máquina (cabeçote XZ) fica pra módulo separado;
  sem Supabase por enquanto (calibração no navegador); sem testes de UI
  (README registra a ausência).
- **Pendências em aberto:** publicar/build de produção com a aba Huron;
  confirmar no aparelho físico o toque/instalação PWA; cabeçote da segunda
  máquina; formato circular (chanfro/raio); famílias UNC/UNF/BSP;
  17 warnings de lint pré-existentes; limpeza do legado classe E
  (GCodePage/HeidenhainPage desmontadas).

## Publicação: logo oficial, home e commits pendentes- **Implementado:** logo oficial no Header (256px) + ícones PWA regenerados
  do logo (192/512/180); página inicial com cards dos 4 módulos (layout do
  app irmão, paleta do CNC); aba Início como padrão; seletor de calibração
  em 4 posições fixas + chave por máquina (`feller-huron45`) com migração.
- **Corrigido:** build PWA que quebrava com o logo de 2MB (limite do
  precache) via derivado 256px; inputs mobile sem menos (type text +
  inputMode + botão +/− à esquerda + vírgula decimal); tag OFFLINE e texto
  HEIDENHAIN removidos do Header; título CNC OPERADOR centralizado.
- **Decisões:** commits separados por tema antes do push (Huron, logo,
  home+diário); 3 PNGs da raiz seguem fora do Git; pasta `docs/diario-de-bordo/`
  entra no versionamento.
- **Pendências em aberto:** validar no celular a home e o toque; decidir
  destino final dos 3 PNGs; aposentar `scripts/generate-icons.mjs` (sobrescreve
  os ícones reais se rodado).

## Ajustes finos da home + mobile do Huron
- **Implementado:** linha "Máquina: Portal Feller" no card do Huron na home
  (subtítulo ajustado pra não duplicar); botão +/− à esquerda dos campos;
  teclado mobile com menos e vírgula decimal.
- **Decisões:** verificação mobile por inspeção (sem aparelho iOS disponível);
  calibração fica no aparelho (localStorage), sem servidor.
- **Pendências em aberto:** teste físico no iPhone (instalação PWA, offline,
  toque); cabeçote da segunda máquina.

## Controle de acesso Beta (estrutura pronta, backend pendente)- **Implementado:** `features/auth/` (client Supabase, `AuthProvider` com
  revalidação a cada 5 min, login, tela de bloqueio, portão na entrada do
  `App`); `.env.example`; secrets `VITE_*` no workflow; sem auto-cadastro,
  mensagens genéricas anti-enumeração. Zero toque em cálculo/Huron/previews.
- **Corrigido no caminho:** colisão de nomes `authContext.js` ×
  `AuthContext.jsx` no Windows + imports do hook não atualizados (build
  quebrou 2x, resolvido com `auth-context.js` + imports em `./useAuth`).
- **Decisões:** fonte da verdade é o banco (`app_users` relido por sessão);
  RLS só-leitura própria; bypass técnico do bundle aceito como teto
  (calculadora local, sem dados alheios); admin inicial pelo dashboard.
- **Pendências em aberto:** criar projeto Supabase + rodar SQL + cadastrar
  admin e betas + secrets no GitHub (passo a passo entregue, sem acesso
  daqui); commit/push desta feature.

## Tolerância offline do Beta (7 dias)
- **Implementado:** `offlinePolicy.js` (`OFFLINE_TOLERANCE_MS` centralizado,
  guarda só `{uid, granted, at}`); `checkAccess` com `try/catch`: online o
  banco decide e renova/limpa a concessão; offline usa a concessão se for do
  mesmo usuário e estiver na janela, senão bloqueia com mensagem de conexão;
  primeiro acesso exige rede; `signOut` limpa a concessão; flag `offline` no
  contexto; mensagem dedicada em `BlockedPage` e no login offline.
- **Decisões:** revogação só detectável online (aceito); janela puramente
  temporal (7 dias do último check válido).
- **Validação:** 203/203, build OK, lint 0 erros. Sem commit/push.
