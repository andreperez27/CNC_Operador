# Copie para .env.local (nunca commitar — já coberto pelo .gitignore)
# e preencha com os dados do projeto Supabase (Settings → API).
VITE_SUPABASE_URL=https://xyzcompany.supabase.co
VITE_SUPABASE_ANON_KEY=sua-anon-key-publica
